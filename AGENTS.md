# AGENTS.md

## Cursor Cloud specific instructions

This repository is a **GitHub Profile README** — it contains only `README.md` (with HTML/Markdown content) and `LICENSE`. There is no application code, no dependencies, no build system, no tests, and no services to run.

### Key points

- **No dependencies**: No `package.json`, `requirements.txt`, or any dependency manifest exists.
- **No build/lint/test**: There are no build steps, linters, or test suites configured.
- **No services**: Nothing needs to be started or run.
- **Core content**: The `README.md` uses HTML within Markdown for layout (centered headings, image badges, social links, GitHub stats). To verify changes, render the Markdown in a browser or use GitHub's preview.
- **Validation**: To check that `README.md` is well-formed, you can open it in a browser or use a Markdown preview tool. There is no automated validation configured.
